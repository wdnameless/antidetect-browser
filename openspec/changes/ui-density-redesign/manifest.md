# Requirements Manifest — ui-density-redesign

Source: user message 2026-09-14. Every requirement carries the VERBATIM words it came from.
Status: `open` | `in-spec` | `in-ticket` | `done` | `placeholder` | `deferred` | `dropped`.

## Verbatim source (user's own words)

> «Я хочу такой же интерфейс как shardxbrowser сейчас у нас он слишком нагромажденный и много визуальных багов, нужен минимализм и удобные меню чтобы не урезать функционал, используй т3 воркфлоу и дизайн скиллы чтобы сделай удобный UI UX. Так же иконка не такая как я задавал, должно быть так»

> Wave 0 answers: «7 верхних + под-табы внутри», «Оставить тёмную, отполировать», «Оставить монохром, акцент = белый», «Плотно: 48–56px, одна строка», «Оболочка + все табличные страницы», «Везде: exe/tray/favicon/icns + марка в сайдбаре».

## Requirements

| # | Requirement | Verbatim quote | Status |
|---|---|---|---|
| R01 | Navigation collapses to ~7 top-level items; the rest become contextual sub-tabs inside a section, so no page becomes unreachable | «удобные меню чтобы не урезать функционал» + «7 верхних + под-табы внутри» | done |
| R02 | No functionality is lost or removed by the redesign | «чтобы не урезать функционал» | done |
| R03 | Profile/list rows become single-line, 48–56px tall (currently 100px, two storeys) | «Плотно: 48–56px, одна строка» | done |
| R04 | The UI stops reading as cluttered — measured as full-perimeter bordered containers, duplicated chrome, and dead CSS | «он слишком нагромажденный» | done |
| R05 | Visual bugs are found and fixed | «много визуальных багов» | done |
| R06 | Typography, spacing and controls carry a real scale instead of ad-hoc hardcoded px | «нужен минимализм» | done |
| R07 | Dark theme stays; it is polished rather than replaced | «Оставить тёмную, отполировать» | done |
| R08 | The palette stays monochrome; the accent is white | «Оставить монохром, акцент = белый» | done |
| R09 | Scope covers the app shell plus all table-style pages (Profiles, Proxies, Devices, Extensions, Groups, Trash) | «Оболочка + все табличные страницы» | done |
| R10 | The brand icon becomes the user's mark — the black visor-mask with eye slits — replacing the indigo shield generated by `scripts/generate-app-icon.mjs` | «иконка не такая как я задавал, должно быть так» | done |
| R11 | The icon is applied everywhere: `.exe`, tray, favicon, `.icns`/`.ico`, and the sidebar brand mark | «Везде: exe/tray/favicon/icns + марка в сайдбаре» | done |
| R12i | Icon must stay legible at 16px (tray/favicon) — implies a simplified variant, not a straight downscale | «Везде: exe/tray/favicon/icns» | done |
| R13i | Operator-chosen colours (profile/tag colour pickers) survive the redesign untouched | prior decision, restated by «не урезать функционал» | done |
| R14i | Every table page keeps its existing capabilities (search, filters, pagination, batch actions, row actions) | «чтобы не урезать функционал» | done |

## Superseded prior decisions

`openspec/changes/nulltrace-noir-page-sweep` recorded two deferrals that this message cancels.
Silence never cancels a requirement, but the user's own words here do:

| Prior row | Prior text | Status | Why it no longer binds |
|---|---|---|---|
| R69i | «ShardX-style sectioned Profiles form with counters — offered, not chosen» | dropped | The user now asks verbatim for «такой же интерфейс как shardxbrowser» with «Плотно: 48–56px, одна строка»; the offer is accepted by this message. |
| R77i | «no page re-architecture» | dropped | Explicitly reversed: «нужен минимализм … не урезать функционал» plus the density answer requires restructuring the profile row. |

## Verified facts gathered before planning (not assumptions)

- The renderer currently paints **0 chromatic colours across 19 distinct values** — the
  monochrome sweep is genuinely complete. Measured in the running app, not read from source.
- Profile rows measure exactly **100px**; there are 3 of them and all are 100px.
- A live `.topbar` is 48px; `.page-header` (64px) does **not** render, yet its rules and a
  second `.window-controls` block remain in the 1614-line stylesheet as dead weight.
- **55 elements** draw a full-perimeter border.
- The sidebar is 220px with **15 nav items** and 44 buttons on one screen.
- Sidebar icons are 25 hand-rolled inline SVGs in `icons.tsx`.

## Out of scope (recorded, not scheduled)

- Light theme — the user chose «Оставить тёмную».
- FlowCanvas internal re-architecture (a 2994-line canvas): it inherits new tokens only.
- Per-page visual redesign of Settings sub-sections beyond token compliance.
