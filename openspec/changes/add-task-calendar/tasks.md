## 1. Projection and page

- [ ] 1.1 Implement `cronProjection.ts` (next-occurrence projection, standard 5-field dialect, invalid-expression tolerance) with unit tests.
- [ ] 1.2 Implement `Calendar.tsx` month grid reading triggers + task groups, marker cells (+N overflow), group drawer on click; i18n keys RU/EN.
- [ ] 1.3 Register route + sidebar entry (integration owner slot); visual verification pass (Playwright screenshot per repo standards).

## 2. Verification

- [ ] 2.1 Full vitest suite + typecheck green; CHANGELOG entry; `openspec validate add-task-calendar --strict`.